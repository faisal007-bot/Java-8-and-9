package features.functional;

//import java.util.function.BinaryOperator;
//import java.util.function.DoubleBinaryOperator;
//import java.util.function.DoubleUnaryOperator;
//import java.util.function.IntBinaryOperator;
//import java.util.function.IntUnaryOperator;
//import java.util.function.LongBinaryOperator;
//import java.util.function.LongUnaryOperator;
//import java.util.function.UnaryOperator;

public class MyOperators {
	public static void main(String[] args) {
//		if the type of parameters of argument and return type then we can use the unaryoperator
//		UnaryOperator<Integer> u = a->a*a;
//		System.out.println(u.apply(10));
		
//		used if both the input and output is of int type
//		IntUnaryOperator i = a->a+a+a;
//		System.out.println(i.applyAsInt(47));
		
//		used if both the input and output is of long type
//		LongUnaryOperator l = a->a+a;
//		System.out.println(l.applyAsLong(74L));
		
//		used if both the input and output is of double type
//		DoubleUnaryOperator d = a->a*a+100;
//		System.out.println(d.applyAsDouble(45.5d));
		
//		if the input and output type is same and we want to  pass 2 input parameters then we
//		must use the binary operator
//		BinaryOperator<Integer> b = (n1,n2)->n1*n2;
//		System.out.println(b.apply(12, 20));
		
//		if the input type of both the parameters and output type of the parameter is int then 
//		we must use this 
//		IntBinaryOperator i = (n1,n2)->n1*n2;
//		System.out.println(i.applyAsInt(47, 48));
		
//		if the input type of both the parameters and output type of the parameter is long then 
//		we must use this 
//		LongBinaryOperator l = (n1,n2)->n1*n2;
//		System.out.println(l. applyAsLong(20, 10));
		
//		if the input type of both the parameters and output type of the parameter is double then 
//		we must use this 
//		DoubleBinaryOperator d = (n1,n2)->n1*n2;
//		System.out.println(d. applyAsDouble(20d, 10.5d));
	}
}
